<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :student_name %>
  </div>

  <div class="field">
    <%= form.label :project_links %>
    <%= form.text_area :project_links, id: :student_project_links %>
  </div>

  <div class="field">
    <%= form.label :contact_email %>
    <%= form.text_field :contact_email, id: :student_contact_email %>
  </div>

  <div class="field">
    <%= form.label :profile %>
    <%= form.text_area :profile, id: :student_profile %>
  </div>

  <div class="field">
    <%= form.label :desired_pay %>
    <%= form.number_field :desired_pay, id: :student_desired_pay %>
  </div>

  <div class="field">
    <%= form.label :part_time %>
    <%= form.number_field :part_time, id: :student_part_time %>
  </div>

  <div class="field">
    <%= form.label :full_time %>
    <%= form.number_field :full_time, id: :student_full_time %>
  </div>

  <div class="field">
    <%= form.label :contract_work %>
    <%= form.number_field :contract_work, id: :student_contract_work %>
  </div>

  <div class="field">
    <%= form.label :skills %>
    <%= form.text_area :skills, id: :student_skills %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
